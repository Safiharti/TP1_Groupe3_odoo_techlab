<odoo>
    <!-- Action pour la liste des saisons -->
    <record id="action_season" model="ir.actions.act_window">
        <field name="name">Seasons</field>
        <field name="res_model">sale.season</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{}</field>
    </record>

    <!-- Action pour la liste des performances des produits -->
    <record id="action_product_performance" model="ir.actions.act_window">
        <field name="name">Product Performance</field>
        <field name="res_model">product.performance</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{}</field>
    </record>

    <!-- Action pour la liste des fidélités des clients -->
    <record id="action_customer_loyalty" model="ir.actions.act_window">
        <field name="name">Customer Loyalty</field>
        <field name="res_model">customer.loyalty</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{}</field>
    </record>

    <!-- Action pour la liste des performances des employés -->
    <record id="action_employee_performance" model="ir.actions.act_window">
        <field name="name">Employee Performance</field>
        <field name="res_model">employee.performance</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{}</field>
    </record>

    <!-- Action pour la liste des évaluations de satisfaction -->
    <record id="action_satisfaction" model="ir.actions.act_window">
        <field name="name">Satisfaction</field>
        <field name="res_model">sale.satisfaction</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{}</field>
    </record>

    <!-- Action pour la liste des recommandations de produits -->
    <record id="action_product_recommendation" model="ir.actions.act_window">
        <field name="name">Product Recommendations</field>
        <field name="res_model">product.recommendation</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{}</field>
    </record>

        <!-- Action -->
    <record id="action_product_performance" model="ir.actions.act_window">
        <field name="name">Product Performance</field>
        <field name="res_model">product.performance</field>
        <field name="view_mode">tree,form</field>
    </record>
</odoo>
